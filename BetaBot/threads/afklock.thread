--[[
while true do
    local isAFK = bagelBot.getPersistence("isAFK")
    if not isAFK then bagelBot.setPersistence("isAFK", {}) isAFK = {} end
    for k, v in pairs(isAFK) do
        local _, res = commands.tp(v[1], math.floor(v[2])+.5, "~", math.floor(v[4])+.5)
    end
    sleep()
end
--]]
